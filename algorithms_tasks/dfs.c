#include<stdio.h>
#include<time.h>
#include<stdlib.h>
typedef struct VERTEX
{
	int id;
	int dist ;
	struct VERTEX * next ;
} VERTEX ;
void DFS(G,u)
{
	VERTEX*v;
	while ((*v)->next!=NULL)
	{
		c[v]=WHITE
		p[v]=NULL;
		s[v]=NULL;
		t[v]=NULL;
	}
 	time=0;
	DFS-Visit(G; u)
}
DFSVisit(G, u)
{
	c[u]=GREY;
	s[u]=time;
	time++;
	while(each (u; v)inE[G]
	{
		
	}
	if (c[v]==WHITE)
	{
		p[v]=u;
		DFSVisit(G; v);
	}
	c[u]=BLACK;
	t[u]=time;
	time++;
}

